# Local reporting service configuration. The DuckDB artifact is built and
# published by the hyrox_analysis pipeline; fetch it with:
#   python -m pyrox_api_service.fetch_db
PYROX_DUCKDB_PATH="pyrox_duckdb"
PYROX_DB_POINTER_URL="https://d2wl4b7sx66tfb.cloudfront.net/db/latest.json"
