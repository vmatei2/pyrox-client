[pytest]
addopts = -q
testpaths =
    tests
    api/tests
pythonpath =
    src

# Explanation:
# - addopts: quiet output by default to keep CI logs clean.
# - testpaths: discover tests in both client and API folders.
# - pythonpath: add `src` so `import pyrox` resolves without installing.
